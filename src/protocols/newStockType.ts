export type newStockType = {
  stockName: string;
  stockTag: string;
  price: number;
};

export type editStockType = {
  id: number;
  stockName: string;
  stockTag: string;
};

export type stockType = {
  id: number;
  name: string;
  stockTag: string;
};
